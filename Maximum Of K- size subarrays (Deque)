#include<bits/stdc++.h>
typedef long long ll;
using namespace std;
int main()
{
    int n,k;
    cin>>n>>k;
    ll a[n];
    vector<ll> v;
    for(int i=0;i<n;i++)
    cin>>a[i];
    for(int i=0;i<=n-k;i++)
    {
      cout<<*max_element(a+i,a+i+k)<<" ";   
    }
    return 0;
 
}
